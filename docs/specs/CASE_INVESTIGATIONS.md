# CASE Investigations — Classifier Quality (Drill 03, Part A)

> Generated by `src/bin/script.ts` from `docs/specs/classification.jsonl`. Static synthesis for the spec stage — **not** the deliverable.

## TL;DR — the decision read

- **n = 18 tickets** across **4 classes** — small sample; each ticket is ~5.6 pts, so treat every number as directional, not precise.
- **Accuracy 61.1%** — the vanity number.
- **Macro-F1 0.50** — the honest number (weights every class equally). **This is the one to trust.**
- **Weighted-F1 0.56** — support-weighted, sits between the two.

## Key findings (ranked)
1. **`feedback` is invisible to the classifier** — 3 gold tickets, predicted **0** times (recall 0%). Accuracy hides this entirely; macro-F1 is what catches it.
2. **`billing` is the dumping bucket** — predicted 7× but only 6 are truly `billing` (precision 0.57). Wrong-class tickets fall here by default.
3. **Headline accuracy overstates quality**: accuracy 61.1% vs macro-F1 0.50. The 11-pt gap is the imbalance tax — common classes carry the average.

## Per-class scorecard

| class | support | predicted | precision | recall | F1 |
|---|---:|---:|---:|---:|---:|
| `technical` | 5 | 6 | 0.67 | 0.80 | 0.73 |
| `account` | 4 | 5 | 0.60 | 0.75 | 0.67 |
| `billing` | 6 | 7 | 0.57 | 0.67 | 0.62 |
| `feedback` | 3 | 0 | — | 0.00 | — |

_Sorted by F1, worst last — reading down the F1 column *is* the “where does it fail” answer. `—` precision = class never predicted (0/0 undefined)._

## Confusion matrix

Rows = gold (truth), columns = predicted. Diagonal = correct.

| gold ↓ \ pred → | `account` | `billing` | `feedback` | `technical` | recall |
|---|---:|---:|---:|---:|---:|
| `account` | **3** | 1 | · | · | 0.75 |
| `billing` | 1 | **4** | · | 1 | 0.67 |
| `feedback` | 1 | 1 | · | 1 | 0.00 |
| `technical` | · | 1 | · | **4** | 0.80 |

## Every misclassification (7)

| id | gold → predicted | text |
|---|---|---|
| t15 | `account` → `billing` | I want to delete my account and all associated data. |
| t04 | `billing` → `technical` | The receipt PDF won't download, it just spins forever. |
| t05 | `billing` → `account` | Please update the card on file, the old one expired. |
| t16 | `feedback` → `technical` | Love the new dashboard, but a dark mode would be amazing. |
| t17 | `feedback` → `account` | Honestly the onboarding flow could be a lot clearer. |
| t18 | `feedback` → `billing` | Great support last week — just wanted to say thanks! |
| t11 | `technical` → `billing` | Getting rate-limited even though I'm well under my quota. |

## Metric traps this data would spring on a naive read

- **Accuracy under imbalance** — 61% sounds passing; it's carried by `billing`/`technical`. Macro-F1 is the guard.
- **A never-predicted class → precision 0/0** — undefined, not 0. Coding it as 0 silently, or dropping the class, would erase the single most important finding (`feedback` collapse). Shown as `—` and called out explicitly.
- **Tiny n** — 3–6 tickets/class. One flipped label swings a class F1 by ~0.2. Directions are trustworthy; decimals are not. Collect more before trusting any per-class number.
